// astro.config.mjs
import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://oyx1231.github.io', // 你的GitHub Pages域名
  base: '/my-resume', // 你的仓库名，前面带斜杠
  // ...其他配置
});